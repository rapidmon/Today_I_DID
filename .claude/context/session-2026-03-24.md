# 세션 요약 — 2026-03-24

## 프로젝트 상태
- **브랜치**: `main`
- **최신 커밋**: `cb4d74c` (build version)
- **미커밋 변경사항 다수 있음**

## 완료된 작업

### 앱 핵심 버그 수정
1. **할 일 데이터 영속 저장** — `useState` → Zustand + AsyncStorage (`stores/taskStore.ts` 신규 생성). 앱 재시작/백그라운드 복귀 시 데이터 유지
2. **SafeArea 하단 겹침** — `_layout.tsx`에 `SafeAreaProvider` 추가
3. **edge-to-edge 비활성화** — `gradle.properties`에서 `edgeToEdgeEnabled=false`

### 앱 UI 리디자인 (다크 → 라이트 테마)
4. **밝은 테마 적용** — 배경 `#F5F5F8`, 텍스트 어두운 색, 입력창/카드 흰색
5. **루틴 삭제 버튼** — `onLongPress` → ✕ 아이콘 버튼으로 변경
6. **체크박스 UX 개선** — TAP 텍스트 → 빨간 테두리 빈 정사각형, 완료 시 ✓(1초) → 미니블록
7. **미니블록 컴포넌트** — `components/ui/MiniBlock.tsx` 생성, 할 일 옆에 실제 블록 모양 표시
8. **번호뱃지 간소화** — 동그라미 배경 제거, 항상 숫자만 표시
9. **ADD 버튼 통일** — 루틴 모드에서도 "ADD"
10. **루틴 아이콘 위치** — 텍스트 왼쪽 → 오른쪽
11. **성취 모달 블록 표시** — 텍스트(I,T,S) → 해당 색상 1칸 블록

### 위젯 전면 리디자인
12. **레이아웃 재구성** — 게임판(좌 5:1) + 사이드패널(우: NEXT + SCORE)
13. **반투명 배경** — 투명도 0%~80% 5단계 순환 (SharedPreferences 저장)
14. **NEXT 블록 미리보기** — 사이드패널에 다음 블록 표시, 블록 없으면 "+" 아이콘
15. **NEXT 박스 = 앱 열기** — 탭하면 항상 앱 열기
16. **레트로 도트 버튼** — 6겹 레이어 pixel-art 3D 베벨 (btn_retro.xml, btn_retro_blue.xml, btn_retro_dark.xml)
17. **픽셀아트 아이콘** — 좌/우/아래 화살표, 회전, 새로고침 모두 벡터 drawable로 픽셀 스타일
18. **새로고침 비활성화/활성화** — 평소 회색(NOOP), GAME OVER 시 녹색(리셋)
19. **GAME OVER 픽셀아트** — Canvas에 5x7 픽셀 폰트로 직접 렌더링 (빨간색)
20. **글로시 블록** — 상단/좌측 하이라이트 + 하단/우측 그림자 + 글로시 그라데이션
21. **STOCK 제거** — NEXT 박스로 대체

### 문서
22. **plan.md** — 위젯 v2 UI, 히스토리 탭(10장), 랭킹 모드(11장) 추가
23. **CLAUDE.md** — 위젯 리디자인 계획 섹션 추가

## 진행 중인 작업
- 히스토리 탭 구현 (plan.md 10장)
- 랭킹 모드 구현 (plan.md 11장)
- iOS 위젯 Xcode 설정 미완료

## 주의사항
- `android/` 폴더 `npx expo prebuild`로 덮어쓰면 위젯 코드 전부 사라짐
- RemoteViews에서 `View` 태그 사용 불가 → `TextView`로 대체
- `letterSpacing` 속성 RemoteViews 호환성 문제
- git push 금지 — 사용자가 직접
- 빌드/실행 — 사용자가 직접
