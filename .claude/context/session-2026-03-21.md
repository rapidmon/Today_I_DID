# 세션 요약 — 2026-03-21

## 프로젝트 상태
- **브랜치**: `main`
- **마지막 커밋**: `2c10218` — `docs: CLAUDE.md 업데이트 및 위젯 개선 계획 추가`
- **미커밋 변경사항**: 아래 작업들이 아직 커밋되지 않음 (최적화, 리팩토링, score 동기화, 위젯 개선)

### 수정된 파일 (미커밋)
| 파일 | 변경 내용 |
|------|----------|
| `app/index.tsx` | 스타일 분리, useMemo 추가, setTasks 배치 통합, handleAddTask 중복 제거, score 동기화 추가 |
| `stores/gameStore.ts` | `syncScore()` 메서드 추가 |
| `lib/widgetBridge.ts` | `getScore()` 추가, iOS 지원 |
| `lib/tetrisEngine.ts` | moveLeft/moveRight 통합, advanceAnimation dead branch 제거, Set 최적화 |
| `android/.../TetrisWidgetProvider.kt` | [+] 앱열기 버튼, GAME OVER 시 버튼 색상 변경(초록), loadState 중복 제거 |
| `android/.../TetrisGameEngine.kt` | processAction/advanceAnimation이 상태 반환, gridRecordIds 추가, 성취 저장, resetGame 성취 보존 |
| `android/.../TetrisWidgetBridge.kt` | content 파라미터 추가, getScore/getAchievements/isGameOver 메서드 |
| `ios/TetrisWidgetBridge/TetrisWidgetBridge.swift` | getScore 추가 |
| `ios/TetrisWidgetBridge/TetrisWidgetBridge.m` | getScore Obj-C 매크로 추가 |
| `constants/homeStyles.ts` | **새 파일** — index.tsx에서 분리된 스타일 |
| `types/achievement.ts` | **삭제** — 미사용 타입 |

---

## 완료된 작업

### 1. 앱-위젯 브릿지 구현
- React Native ↔ SharedPreferences(Android) / UserDefaults(iOS) 네이티브 브릿지
- 할 일 완료 → 블록이 위젯에 즉시 반영

### 2. 줄 클리어 버그 수정
- 애니메이션 `fade` 단계에서 바로 clearLines + spawnPiece 실행하도록 변경
- 기존: highlight → fade → done (done이 호출 안 됨) → 줄이 제거 안 되고 멈춤

### 3. 위젯 버튼 분리
- **[+]** 버튼: 앱 열기 (PendingIntent.getActivity), GAME OVER 시 비활성화
- **[↻]** 버튼: 일반=동기화(회색), GAME OVER=리셋(초록색 `#00CC00`)

### 4. 성취 시스템 연결
- Kotlin `gridRecordIds` 추가 — 각 셀이 어떤 할 일에서 왔는지 추적
- 줄 클리어 시 `recordMap`에서 content/blockType 조회 → 성취에 포함 저장
- 앱 성취 모달: 아코디언 UI (클릭하면 펼쳐서 할 일 목록 + 블록 타입 표시)

### 5. GAME OVER 처리
- 위젯 새로고침 버튼으로 리셋 (achievements/recordMap 보존)
- 앱 ADD 버튼 비활성화 + "GAME OVER" 배지 표시
- 앱 포그라운드 복귀 시 리셋 감지 → Zustand score도 0으로 동기화

### 6. Score 동기화 수정
- 위젯에서 줄 클리어 시 점수가 올라가지만 앱 Zustand에는 반영 안 되던 문제
- `getScore()` 브릿지 + `syncScore()` Zustand 메서드 추가
- 앱 포그라운드 복귀 시 위젯 score 읽어서 Zustand 동기화

### 7. 테트리스 판 높이 변경
- ROWS: 20 → **12** (4x4 위젯 크기에 적합)

### 8. iOS WidgetKit 코드 작성
- `TetrisGameEngine.swift` — Kotlin 로직 완전 포팅
- `TetrisWidget.swift` — WidgetKit + SwiftUI 위젯 뷰
- `TetrisWidgetRenderer.swift` — SwiftUI Canvas 그리드 렌더링
- `TetrisIntents.swift` — AppIntent 기반 인터랙티브 버튼 (iOS 17+)
- `TetrisWidgetBridge.swift/.m` — React Native ↔ UserDefaults 브릿지
- `SETUP_GUIDE.md` — Mac에서 Xcode 설정 가이드

### 9. 최적화 (/optimize)
- Android loadState 중복 호출 제거 (processAction이 상태 반환)
- setTasks 배치 통합 (리렌더링 2회→1회)
- todayTasks useMemo 메모이제이션
- clearLines에서 Set 사용 (O(n)→O(1))
- FlatList 성능 속성 추가

### 10. 리팩토링 (/refactoring)
- 미사용 `types/achievement.ts` 삭제
- moveLeft/moveRight → moveHorizontal 통합
- handleAddTask Task 생성 중복 제거
- 스타일을 `constants/homeStyles.ts`로 분리 (index.tsx 539줄→420줄)
- advanceAnimation dead branch ('done' case) 제거

### 11. CLAUDE.md 업데이트
- 프로젝트 개요, 기술 스택, 개발 명령어, 아키텍처 전체 반영

### 12. 커밋 정리 (기능별 7개)
```
2c10218 docs: CLAUDE.md 업데이트 및 위젯 개선 계획 추가
8489723 feat(ios): iOS WidgetKit 테트리스 위젯 구현
51f0a90 feat(android): Android 테트리스 위젯 및 브릿지 구현
24193b5 feat(app): 할 일 기록 및 메인 화면 구현
6bb5373 feat: Zustand 게임 상태관리 구현
cbca902 feat: 타입 정의 및 테트리스 엔진 구현
1c18fea chore: Expo 프로젝트 초기화 및 의존성 설정
```

---

## 진행 중인 작업

### 미커밋 변경사항
위 "수정된 파일" 전체가 아직 커밋 안 됨. 다음 세션에서 기능별로 커밋 필요:
1. `refactor: 코드 최적화 및 리팩토링` — 엔진 통합, 스타일 분리, dead code 삭제
2. `feat(widget): 위젯 버튼 분리 및 GAME OVER 처리` — [+], [↻] 색상, 앱 열기
3. `fix: 위젯-앱 score 동기화` — getScore, syncScore

### 알려진 이슈
- **위젯 홈 화면 크기**: `targetCellWidth=4, targetCellHeight=4` 설정했지만 사용자 기기에서 3x5로 표시됨 — 기기/런처별로 다를 수 있음
- **iOS 미빌드**: Swift 코드는 작성했지만 아직 빌드/테스트 안 됨. Mac + Xcode 필요
- **GAME OVER 시 위젯 SCORE 0 리셋**: 코드상으로는 맞지만 사용자가 안 된다고 보고. 실제 테스트 필요

---

## 핵심 결정사항

| 결정 | 근거 |
|------|------|
| 그리드 10x12 (기존 10x20) | 4x4 위젯 크기에 맞춰 높이 축소 |
| 새로고침 버튼 = 리셋 겸용 | 별도 리셋 버튼 대신 GAME OVER 시 색상 변경(초록)으로 구분 |
| [+] 버튼 GAME OVER 시 비활성화 | 리셋 먼저 해야 의미 있음 |
| 성취에 content/blockType 포함 저장 | recordMap으로 매핑, 앱 재시작해도 성취 데이터 유지 |
| iOS 17+ 타겟 | AppIntent 인터랙티브 위젯 필수 |
| 빌드/실행은 사용자가 직접 | Claude는 코드 수정만 (memory에 기록됨) |

---

## 다음 단계

1. **미커밋 변경사항 커밋** (기능별 분리)
2. **Android 빌드 후 테스트** — score 동기화, GAME OVER 리셋, [+] 버튼 동작 확인
3. **위젯 크기 문제 재확인** — 4x4가 안 되면 다른 접근 필요
4. **iOS 빌드** — Mac에서 `npx expo prebuild --platform ios` → Xcode 설정 → 빌드
5. **AsyncStorage 연동** — 현재 tasks/routines가 메모리에만 있어 앱 재시작 시 사라짐

---

## 주의사항

- **git push 금지** — 사용자가 직접 push
- **빌드/실행 명령 금지** — `npx expo run:android` 등은 사용자가 직접 실행
- **android/, ios/ 폴더** — `npx expo prebuild`로 덮어쓰지 말 것 (수동 수정 포함)
- **App Group ID** (iOS): `group.com.limheerae.TodayIdid` — 앱 + 위젯 양쪽에 설정 필수
- **SharedPreferences 키**: `tetris_widget` (Android)
- Windows 환경 (macOS 없음) — iOS 빌드는 공기계 iPhone + Mac 필요
